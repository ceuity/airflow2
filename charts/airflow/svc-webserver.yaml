apiVersion: v1
kind: Service
metadata:
  name: airflow-svc-webserver
spec:
  type: ClusterIP
  selector:
    app: airflow-webserver
  ports:
    - name: webserver-port
      protocol: TCP
      port: 8080
      targetPort: 8080